# AI-拓客（科航 AI 超级员工 · 复刻协作版）功能清单

> 解包自 `AI-拓客(2).zip`，源码目录 `ai-tuoke/`，后端 `server.js`（零依赖 Node，仅用内置 http/fetch/fs 模块，无需 npm install）。
> 数据落在 `ai-tuoke/data/app.db`（SQLite，2026-08-03 起从 JSON 迁移）；三个演示模块仍用 `data/*.json`。
> 合规底线：**不爬取、不模拟点击、不自动发送消息**；所有"获取"与"回复"动作由真人完成，AI 只做解析、分级与草稿生成。

## 一、已实现模块总览

后端共 65 个 API 路由，前端 16 个页面（`server.js` 约 1920 行 / `db.js` 约 990 行 / `app.js` 约 3690 行）。

| 模块 | 入口（左侧导航） | 状态 | 核心能力 |
|---|---|---|---|
| 控制台 Dashboard | 控制台 | ✅ 可用 | 真实数据经营看板（趋势图/分布/平台线索/活动流） |
| 公域拓客 · 地图获客 | 公域拓客 | ✅ 可用 | 高德 POI + 真实地图 + 三级区划 + 勾选导入 |
| 私信获客 | 私信获客 | ✅ 可用 | 手动粘贴私信 → AI 解析线索 → AI 生成预选回复草稿 |
| 话术库 | 话术库 | ✅ 可用 | 常用回复模板管理 + 私信一键插入 |
| 平台接入 | 平台接入 | ✅ 框架就绪 | 抖音/小红书官方 OAuth+Webhook 合规骨架 + AI 意向分级 |
| 公转私 | 公转私 | ✅ 演示 | 公域线索→私域承接流程展示 |
| AI销冠 | AI销冠 | ⚠️ 演示（数值模拟） | 自动加好友/朋友圈/群发，**数值为随机模拟，非真实操作** |
| AI人事 | AI人事 | ✅ 可用 | 简历筛选 + 打招呼 + 问答（JSON 存储） |
| AI客服 | AI客服 | ✅ 可用 | 客服会话管理 + AI 自动回复（JSON 存储） |
| 法务专员 | 法务专员 | ✅ 可用 | 企业法务意见（依据/风险点/建议/法条） |
| 企业智脑 | 企业智脑 | ✅ 可用 | 内容营销/设计创意/数据分析/汇报/团队管理 |
| AI创作 | AI创作 | ✅ 可用 | 关键词/法律/话术/智脑/内容创作 5 类 AI 生成 |
| 客户管理 | 客户管理 | ✅ 可用 | CRM：增删改查/筛选/批量导入去重/统计/跟进/导出 |
| 跟进提醒 | 跟进提醒 | ✅ 可用 | 待办聚合（逾期/今日/即将到期） |
| 状态看板 | 状态看板 | ✅ 可用 | 四列拖拽流转客户状态 |
| 系统设置 | 系统设置（底部） | ✅ 可用 | 密钥配置 + 产品配置 + 测试连接 + 备份/导出/清理/重置 |

> 另有一套**登录鉴权**：登录页 + 右上角账号，角色分 `admin`（管理员）/ `user`（销售）。
> 默认账号 `admin / admin123`、`sales / sales123`（首次启动自动创建，**上线前务必修改**）。

## 二、各模块能做什么

### 1. 控制台 Dashboard（真实数据）
- 统计卡片：累计获客 / 高意向客户 / 平台线索 / 本周新增
- 近 30 天新增客户趋势图（内联 SVG，零外部依赖）
- 来源分布、状态分布、意向等级分布
- 平台线索概览（高/中/低 + 最近线索）、最近客户、真实活动流

### 2. AI 创作（接 DeepSeek deepseek-chat）
5 个子功能：
- **关键词拓展**：基于产品关键词生成抖音/小红书/快手搜索截流词（同义词/长尾/疑问/场景/地域词）
- **法律咨询**：企业法务意见（依据/风险点/建议/法条，结尾含免责声明）
- **客服话术**：按分类与场景生成 5 条客服话术
- **智能脑（智脑）**：内容营销 / 设计创意 / 数据分析 / 汇报 / 团队管理 5 类专家内容
- **内容创作**：短视频脚本 / 朋友圈文案 / 数字人口播 / 带货文案 / 小红书笔记 / 爆款标题，每类 3 条不同角度成品

### 3. 公域拓客 · 地图获客
- 高德 `place/text` POI 搜索，可按城市限定
- 省-市-县**三级联动**（用 adcode 检索下一级），选到哪级就以哪级边界为采集范围
- Leaflet 渲染**真实高德地图**（道路/建筑/POI）+ 标记点 + 行政区紫色边界
- 结果区支持**勾选导入**：勾选若干行只导勾选的，一条不勾导全部

### 4. 私信获客（合规）
工作流：真人从官方 App 手动复制收到的私信 → 粘贴 → AI 解析为结构化线索（昵称/平台/意图/地区/联系方式/标签/意向等级）→ 人工筛选「加入客户库」→ AI 按选定语气生成 **3 条预选回复草稿** → 复制后在平台人工发送
- 顶部有合规横幅："不自动抓取、不自动发送"
- 一键插入话术模板、复制最终回复

### 5. 平台接入（合规自动获客框架）
- 抖音/小红书官方 OAuth 登录跳转 + Webhook 接收推送
- `classifyIntent` 调 DeepSeek 把私信/视频留言/直播发言判为 **高/中/低意向** + 理由 + 标签
- 本地「模拟推送」自测（`/api/platform/simulate`）走真实分级链路，无需凭证/公网
- 线索池支持「加入客户库 / 忽略」
- **真实推送前提**（当前未启用）：企业号/专业号资质 + 已审批私信/评论读取权限 + 公网 HTTPS 部署

### 6. 客户管理（CRM，SQLite 存储）
- 列表：关键词/状态/来源/标签过滤 + 分页
- 新增 / 编辑 / 删除 / 详情抽屉（跟进记录）
- **批量导入**（按"名称+电话"去重）
- **批量打标签**、**批量分配归属销售**
- **AI 智能分析线索**：DeepSeek 对单个客户做意向打分(0-100) + 等级(高/中/低) + 理由 + 微信/短信跟进话术 + 电话外呼脚本，可一键应用到客户（列表显示 AI 评分徽章）
- **客户去重合并**：按电话/名称判重，智能合并（缺值补全、标签/跟进去重、AI 评分取更高）
- **数据导出**：CSV / Excel（16 字段，复用当前筛选条件，UTF-8 BOM 中文不乱码）

### 7. 跟进提醒 / 待办
- 客户可设 `nextFollowAt` 下次跟进时间 + 提醒备注
- `GET /api/todos` 按时间分三档：已逾期 / 今日待跟进 / 即将到期，侧边栏带待办数徽章

### 8. 状态流转看板
- 新客 / 跟进中 / 已成交 / 已流失 四列
- **拖拽流转**：卡片拖到另一列即更新状态（乐观更新 + 失败回滚）

### 9. 话术库（常用回复模板）
- 模板卡片网格，搜索（标题/内容/分类）+ 分类筛选
- 新建 / 编辑 / 删除（二次确认），预置 4 条示例（数据重置后自动恢复）
- 私信获客面板的「一键插入」闭环

### 10. 系统设置
- **DeepSeek 配置**：API Key / Base URL / 模型名 + 「测试连接」真实探测
- **来源标注**：每个配置项显示来自 `.env` 还是页面配置；页面保存的值优先于 `.env`
- **产品业务配置**：产品名 / 描述 / 目标客户 / 核心优势（供 AI 提示词使用）
- **数据工具**：一键备份、全量导出（`data/exports/`）、缓存清理、**数据重置**（自定义弹窗 + 确认口令 `RESET_ALL_DATA` + 可选保留登录账号；重置前自动备份，仅清业务数据）

### 11. 登录鉴权 / 多用户权限
- `POST /api/auth/login` 下发 `session` cookie（HttpOnly + SameSite=Lax，24h）
- 会话存服务端内存 Map，**服务重启需重新登录**
- 角色分级：`admin` / `user`；管理类接口仅管理员可访问
- 密码以 `sha256:<hex>` 哈希入库（启动时自动迁移历史明文，登录兼容旧密码并自动升级）

## 三、技术要点
- **后端**：`server.js`，零依赖 Node.js（仅内置 http/fetch/fs/crypto），端口默认 3000（可用 `PORT` 环境变量覆盖）
- **数据层**：`db.js`，使用 Node 内置 `node:sqlite`，DB 路径 `data/app.db`，**WAL 模式**
  - ⚠️ 启动**必须带标志**：`node --experimental-sqlite server.js`（或 `npm start`），Node 22 下该模块仍是实验特性
- **前端**：`index.html` + `style.css` + `app.js`，单页应用，无构建步骤
- **外部依赖**：DeepSeek（AI）、高德地图 Web 服务（POI/区划）、Leaflet（地图渲染，CDN）
- **坐标系**：高德 GCJ-02，标记直接可用

## 四、运行方式（30 秒上手）
```bash
cd ai-tuoke
cp .env.example .env        # 填入 DEEPSEEK_API_KEY 与 AMAP_KEY
npm start                   # 等价于 node --experimental-sqlite server.js
# 浏览器打开 http://localhost:3000
# 默认登录：admin / admin123（管理员）、sales / sales123（销售）
```
> 注：DeepSeek Key 是 AI 创作/私信解析/意向分级的必填项；高德 Key 仅地图获客需要。前端改完记得 `Ctrl+Shift+R` 强刷（当前资源版本 `?v=10`）。

## 五、尚未完成（详见 `项目进度.md`）
- [ ] 平台接入真实推送（待企业号资质 + 公网 HTTPS 部署）
- [ ] 密码哈希由 `sha256`（无盐）升级为 `scrypt`（仍是零依赖）
- [ ] 会话持久化（当前存内存，重启失效；需要"记住我"须落库或改签名 token）
- [ ] 演示模块数据层统一（`xiaoguan` / `kefu` / `renshi` 仍用 JSON）
- [ ] 默认账号强制改密（`admin123` / `sales123`）
- [ ] 自动备份（当前仅手动触发）
- [x] ~~登录鉴权 / 多用户权限~~ ✅ 已完成
- [x] ~~数据自动备份~~ ⭕ 已提供手动一键备份

## 六、合规边界（务必遵守）
- ❌ 不使用爬虫、不模拟点击、不自动发送消息
- ✅ 平台获客仅走官方开放平台 OAuth + Webhook（用户授权、平台主动推送）
- ✅ 高德 POI 数据仅限自身业务，不可转卖
- ✅ 客户个人信息处理须有合法授权，遵守《个人信息保护法》
- ⚠️ AI销冠等演示模块的**数值指标为模拟值**，对外演示时须说明，不可当作真实业绩数据
